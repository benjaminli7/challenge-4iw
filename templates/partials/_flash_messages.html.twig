{% for type, messages in app.session.flashbag.all %}
    {% for message in messages %}
        <div style="text-align: center" class="alert-{{ type }}-lg mb-6" role="alert">
            {{ message|trans({}, 'messages') }}
        </div>
    {% endfor %}
{% endfor %}
