{% extends 'base_front.html.twig' %}

{% block title %}Profile update!{% endblock %}

{% block body %}
    {{ form_start(form) }}
        {% for row in form %}
            {{ form_row(row, {'row_attr': {'class': 'custom-class'}}) }}
        {% endfor %}
        <input type="submit" value="S'enregistrer">
    {{ form_end(form) }}
{% endblock %}
